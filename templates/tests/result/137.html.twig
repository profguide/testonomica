{#{% extends('tests/result.html.twig') %}#}

{% block result %}
    {% if sum >= 8 and sum <= 10 %}
        <p><b>У вас отличные знания об Эвересте!</b></p>
    {% elseif sum >= 5 and sum <= 7 %}
        <p><b>У вас средние знания об Эвересте</b></p>
    {% elseif sum >= 0 and sum <= 4 %}
        <p><b>Ваши знания об Эвересте ниже среднего. Тем не менее, вам удалось верно ответить на некоторые вопросы!</b></p>
    {% endif %}

    <p>{% trans %}Correct answers{% endtrans %}:</p>
    <div class="progress">
        <div class="progress-bar" role="progressbar" style="width: {{ scale }}%;"
             aria-valuenow="{{ scale }}"
             aria-valuemin="0"
             aria-valuemax="100">{{ sum }} {% trans %}out of{% endtrans %} 10
        </div>
    </div>
{% endblock %}